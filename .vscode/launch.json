{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/build/tests/StepTimingGenerator_Tests",
            "args": ["--gtest_break_on_failure"],  // Example for Google Test
            "cwd": "${workspaceRoot}",
            "initCommands": [],  // Custom LLDB commands can be placed here
            "terminal": "integrated"  // This controls the terminal used
        }
    ]
}
